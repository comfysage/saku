name="$(basename $2)"
sakudir="${SAKUPATH:-"$HOME/.saku"}"
flaskfile="$sakudir/flask/$1/$2.fl"
srcdir="$sakudir/repo/$name"
store="$sakudir/store"
storedir="$store/$name"

pkgname=""
pkgdesc=""
url=""
depends=()
makedepends=()
provides=()

prepare() {
  trace "preparing $pkgname"
}

build() {
  trace "building $pkgname"
}

package() {
  trace "packaging $pkgname"
}
